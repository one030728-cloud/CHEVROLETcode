{
  "name": "chevrolet-toss-pos-tab-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "zip": "powershell -NoProfile -ExecutionPolicy Bypass -File package.ps1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@tossplace/pos-plugin-sdk": "^0.0.27"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
